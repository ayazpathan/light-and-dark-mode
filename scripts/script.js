const toggelSwitch = document.querySelector('input[type="checkbox"]');

// Swich Theme Dynamically
function switchTheme(event) {}

// Event Listener to listen for change
toggelSwitch.addEventListener("change", switchTheme);
